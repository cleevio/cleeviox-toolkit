---
'@cleeviox/biome': patch
---

nextjs.jsonc enables `css.parser.tailwindDirectives` — Next.js projects style with Tailwind v4 (`@theme`, `@source`, `@custom-variant`), which the CSS parser otherwise rejects in every `globals.css`.
